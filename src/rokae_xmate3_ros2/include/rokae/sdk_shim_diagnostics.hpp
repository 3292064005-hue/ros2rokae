#ifndef ROKAE_SDK_SHIM_DOMAIN_SDK_SHIM_DIAGNOSTICS_HPP
#define ROKAE_SDK_SHIM_DOMAIN_SDK_SHIM_DIAGNOSTICS_HPP

// Diagnostics/runtime compatibility surface extracted from the monolithic sdk_shim entrypoint.
// This header is an incremental migration aid. Existing code may still include
// rokae/sdk_shim.hpp directly, while new code can depend on a narrower domain entrypoint.

#include "rokae/sdk_shim.hpp"

#endif
